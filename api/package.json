{
  "name": "nexo-api",
  "version": "0.1.0",
  "description": "NEXO API — Collective learning protocol for AI agents",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "dependencies": {
    "itty-router": "^5.0.24"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
